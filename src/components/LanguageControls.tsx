// Os controles de idioma e zoom foram movidos para o Navigation (header),
// resolvendo o overlap com o conteúdo das páginas. Este componente é mantido
// como no-op para preservar compatibilidade com as páginas que ainda o importam.
const LanguageControls = () => null;
export default LanguageControls;
